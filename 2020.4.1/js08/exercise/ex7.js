let arr = [1, 5, 6, 7]
let arr2 = [2, 3, 4]

/**
 * 将第二个数组 插入到第一个数组中的index位置
 * @param {Array} arr 源数组
 * @param {Array} arr2 插入数组
 * @param {Number} index  插入的index
 * @returns {Array} 
 */
function fn(arr, arr2, index) {
  return newArr
}

console.log(fn(arr, arr2)) //[1, 5, 6, 7, 2, 3, 4]
console.log(fn(arr, arr2, 1)) //[1, 2, 3, 4, 5, 6, 7]
console.log(fn(arr, arr2, 2)) //[1, 5, 2, 3, 4, 6, 7]