$().ready(() =>{
    function Puzz(leave){
        this.leave = leave;         //定义难度属性
        this.step = 0;              //初始步数
        this.userTime = 0;          //初始时间
                      
    }

    // 获取传入的难度，将图片切割成不同的个数
    Puzz.prototype.pattern = function (){

    }
})